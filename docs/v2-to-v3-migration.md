# Guide v2 → v3 migration checklist

*Last updated: 2026-04-15 (Phase 6)*

Per-chapter disposition for the migration from `guide-v2/` (OpenClaw-era) to `guide-v3/` (Clawford-native). Dispositions:

- **migrate** — port v2 content, strip OpenClaw scar tissue, keep the useful parts
- **rewrite** — v2 version is mostly OpenClaw-era scar tissue; start fresh in v3
- **net-new** — didn't exist in v2; drafted directly in v3
- **archive** — leave in v2 as historical record; don't carry forward

Status values: **pending**, **in_progress**, **done**.

## Checklist

| v2 Ch | v2 file | Disposition | v3 Ch | v3 file | Status | Phase | Notes |
|-------|---------|-------------|-------|---------|--------|-------|-------|
| 01 | `index.md` | migrate | 01 | `01-what-is-clawford.md` | **done** | Phase 7d | Chapter split out from v3 `index.md` (which is now TOC-only) and renamed for consistency with the NN-title.md filename convention. TOC table moved entirely to v3 index.md; chapter prose landed here. "Stitched together with OpenClaw" reframed as "stitched together with plain host crontab, codex, Dropbox, Telegram, Python". Architecture diagram redrawn: OpenClaw gateway block removed, replaced with a "host crontab + codex" pair plus the six agents as plain Python consumers of both. Cross-references bumped: v2 "Ch 02 Before you start" → v3 Ch 03, "Ch 03 VPS setup" → Ch 04, "Ch 05 Infra setup" → Ch 06, new reference to Ch 07 intro-to-agents. VPS-rebuild bullet ("Terraform + Docker, 40 minutes") updated to "Terraform-provisioned and configuration-managed". "Five coworkers" count updated from v2's "six" (Rudolf relay retired). Voice fix: "our family calendar" → "my family's calendar" per guide voice memory. |
| — | — | net-new | 02 | `02-what-clawford-isnt.md` | **drafted** | Phase 0 → Phase 7 (receipts) | The manifesto chapter. Section 9 "What the migration looked like" is a placeholder until Phase 7. |
| 02 | `02-before-you-start.md` | migrate | 03 | `03-before-you-start.md` | **done** | Phase 7d | Migrated with chapter cross-refs bumped (v2 Ch 03 → v3 Ch 04, v2 Ch 05 → v3 Ch 06, v2 Ch 02 → v3 Ch 03 self). OpenClaw-era framing stripped: removed the Hetzner-install and terraform-community-module links (both were OpenClaw docs), reframed "OpenClaw runs with broad filesystem access" as "a Clawford fleet runs...", added a one-line historical note on the `openclaw` Unix user, dropped the "after each OpenClaw upgrade" cadence line. Rewrote "Picking your LLM provider" — post-liberation Clawford has a single backend (`codex` via ChatGPT Plus), so the multi-provider table is gone in favor of a three-bullet rationale, an explicit `llm.py`-swap instruction for operators who want a different provider, and a prominent warning against wiring agents against Claude Pro/Max consumer subscriptions. Added a new "Ch 05 — Dev setup" see-also link that didn't exist in v2. |
| 03 | `03-vps-setup.md` | migrate | 04 | `04-vps-setup.md` | **done** | Phase 6 | OpenClaw gateway install section replaced with `codex login` + SCP `~/.codex/auth.json` pattern. GHCR PAT step removed. Smoke test items replaced with `codex --version` / `codex infer 'say hi'`. Pre-liberation note flags the vestigial Docker install. |
| 04 | `04-dev-setup.md` | migrate | 05 | `05-dev-setup.md` | **done** | Phase 7d | Migrated with chapter cross-refs bumped (v2 Ch 02 → v3 Ch 03, v2 Ch 05 → v3 Ch 06, v2 Ch 06 → v3 Ch 07). OpenClaw-specific framing stripped: "OpenClaw 2026.4.11 exec preflight" reframed as "an earlier platform version's exec preflight" with a pointer to Ch 02's opening scene. Dead "agent test harness" section (T1-T6, ~/openclaw-tests/, tests/setup-tests.sh) replaced with a new "test surface" section describing the live three-layer pytest structure post-Phase-7. Deploy-tool test count bumped from "19 modules, 300+ cases" to 425+ with 9 safeguards. Grafted in Ch 99 Lesson D (Windows CRLF trap) as a new "Windows dev box" section, with both the short-term `tr -d '\r'` workaround and the long-term `.gitattributes *.sh text eol=lf` fix, plus a new Pitfall callout. |
| 05 | `05-infra-setup.md` | **rewrite** | 06 | `06-infra-setup.md` | **done** | Phase 5 (+ Phase 7d graft + Safeguard 8/11 retirement polish) | v3 describes the shared library (`agents/shared/*`), shared brain (git + Dropbox split), Clawford-native deploy.py with 9 active safeguards (Safeguards 8 and 11 both retired with tombstones), and the host-cron runtime. OpenClaw gateway framing dropped. 2026-04-15: grafted in Ch 99 Lesson B (wrapper exec bits in git) as an inline callout in the host-cron section, Lessons E + F (full-agent-id discipline + file-based opt-out pattern) as a new "Retiring an agent" subsection, and rewrote the Safeguard 8/11 section to reflect their retirement in Phases 5 and 7a. |
| 06 | `06-intro-to-agents.md` | **rewrite** | 07 | `07-intro-to-agents.md` | **done** | Phase 6 (+ Phase 7d graft) | "Two kinds of crons" collapsed into "host crons only + 5 AM PT fleet path." "Exec-approvals, and why mine are permissive" replaced with a three-layer defense-in-depth paragraph pointing at OS-level immutability, the script contract, and the `deploy.py` safeguards. Eight workspace files reframed as durable identity loaded on demand, not auto-loaded per cron tick. Script contract and LLM-vs-deterministic sections preserved. 600s-LLM-cron-budget and BOOTSTRAP.md-split-brain pitfalls dropped (both OpenClaw-specific). 2026-04-15: grafted in Ch 99 Lesson A as "How a deploy actually moves code to production" — the canonical SSH+pull+deploy path, the dead-laptop-mirror trap, dirty-tree handling, and Safeguard 4 drift recovery. |
| 07-0 | `07-0-your-first-agent.md` | migrate | 07-0 | `07-0-your-first-agent.md` | **done** | Phase 7d | v2's eight-step arc collapsed to seven steps. Step 6 "Pair the bot with OpenClaw" (`oci agents add` + `/start` + `oc pairing approve`) removed entirely — the OpenClaw gateway is gone and there's nothing to pair with. Step 5 "git pull on the VPS" and Step 7 "run deploy.py" merged into a single step 6 "commit, push, pull, deploy" that points at Ch 07's canonical deploy-path section. New step 5 "Register the agent's host crons in `ops/scripts/install-host-cron.sh`" inserted — the v2 flow assumed OpenClaw cron registration via the manifest, but post-liberation `deploy.py` does not touch the crontab and the install script is the single source of truth. New Pitfall added for "forgetting to register host crons". Two-rules section cut to one-rule section: "Never hand-edit openclaw.json" removed (the file doesn't exist post-liberation); "never test experimental features on a live channel" reframed with generic examples (new cron schedule, shared-library module, LLM prompt, MFA flow) instead of OpenClaw-specific ones (ACP, plugins). Entire "Why Clawford skips OpenClaw's onboarding" subsection deleted — the BOOTSTRAP.md split-brain problem is historical scar tissue that no longer applies. Cross-refs bumped. "Ten safeguards" → "nine safeguards". |
| 07-1 | `07-1-mr-fixit.md` | migrate | 07-1 | `07-1-mr-fixit.md` | **done** | Phase 7d | Migrated from v2 + scar-tissue enhancement from research/agent-history-fix-it.md. Major structural change: promoted the probation episode from a single Pitfall callout to a full narrative section ("The probation episode"), ~1000 words covering the 2026-04-11 chr() approval incident, the three rule violations, the brain transplant (commit 945ee28), the five-rule Diagnostic Discipline section, the P1-P4 failure framework, and the pre-baked retire.sh. The v2 "eight silent failures" first-deploy war story was trimmed — five of the eight were OpenClaw-specific (BOOTSTRAP.md shadow, oci agents add OAuth wizard, policy=null default, BASH_SOURCE trick, openclaw.json SCP bricking) and no longer apply post-Phase-7; the three that generalize (Windows CRLF, parser format drift, status-file three-layer root cause) are preserved as inline scar tissue with pointers at guide-v2 for the full pre-liberation story. Cron surface table rewritten: all 12 fix-it crons now run from the host crontab as pure Python via install-host-cron.sh; no more in-container LLM dispatch queue, no more `oc cron list`. The three host wrappers (fleet-health-host.sh, morning-status-host.sh, morning-fleet-deliver-host.sh) are now described as first-class parts of Mr Fixit's cron surface rather than adjacent-but-separate host crons. Cross-references bumped. "Ten safeguards" → "nine safeguards". Pitfall on openclaw.json SCP removed (file doesn't exist). Pitfall on ACP binding reframed as "persistent-session protocol" (the ACP specifics are pre-liberation). |
| 07-2a | `07-2a-lowly-worm-newsfeed.md` | migrate | 07-2a | `07-2a-lowly-worm-newsfeed.md` | **done** | Phase 7d | Migrated from v2 with preference-learning narrative preserved intact (it was already the narrative spine in v2 and didn't need promotion like Mr Fixit's probation story). `openclaw infer` → `agents.shared.llm.infer`. `oc cron list` / `oc cron run` replaced with host-cron wrapper scripts. New section "The Phase 3 pilot context" added to the deployment walkthrough explaining Lowly's role as the first agent migrated to the post-liberation stack and the two load-bearing patterns that came out of that migration (the 600-second LLM cron timeout → compose/deliver split, the sys.path shim for shared library imports). Telegram account lore (named vs default) removed — post-liberation there's only per-agent bot tokens. Cron table reframed: "LLM" column now means "the cron calls agents.shared.llm.infer internally" not "the cron runs inside an LLM session". New Pitfall added on Google News URL decoding. Paths updated to ~/.clawford/. Cross-refs bumped. |
| 07-2b | `07-2b-lowly-worm-social.md` | migrate | 07-2b | `07-2b-lowly-worm-social.md` | **done** | Phase 7d | Migrated from v2 with the smart-reply chip incident (2026-04-14) promoted from a Pitfall callout to a full narrative section ("The smart-reply chip incident") covering what went wrong (Alexis 5x DM auto-reply to a dormant thread), the three-layer fix (direct URL navigation, bare-URL guard, network send-block via `page.route`), the regression tests (`test_scrape_messages_navigates_by_url_never_clicks` and `test_scrape_messages_skips_enrichment_for_bare_messaging_url`), and the general lesson about network-level defense-in-depth for any Playwright code path that mutates remote state. The four-surface structure (feed, notifications, DMs, profile viewers) preserved. `openclaw infer` → `agents.shared.llm.infer`. `oc cron` → host-cron wrapper paths. The Docker+socat pitfall dropped entirely (container is gone post-liberation; keepalive runs as a plain host cron now). Paths updated to ~/.clawford/. Cross-refs bumped. Voice grips re-ran after promotion — two `\bwe\b` fixes in the narrative section and one in a Pitfall ("one item missed"/"the whole cron missed" reframe). |
| 07-3 | — | net-new | 07-3 | `07-3-mistress-mouse.md` | **done** | Phase 7d | Drafted from `research/agent-history-family-calendar.md` — the first agent in a Clawford fleet to run browser-based auth, so the Google OAuth pattern is promoted to its own named section and reused verbatim from Sergeant Murphy + Huckle Cat. Structure: TL;DR, Meet the agent, Why you'd want / skip, What makes this hard, The Google OAuth pattern (five rules — desktop creds, test users, run-locally-then-SCP, refresh token forever, scope upgrades need re-auth), The WhatsApp chapter (the Baileys ban-risk section with the three-rule contract — outbound to Telegram only, dmPolicy=disabled, one account per deployment), The routing boundary with Sergeant Murphy (Workflowy-presence = meeting, commit `91d248a`), Current state (6 host crons + 6 I/O scripts), Deployment walkthrough with Google Cloud pre-steps and optional WhatsApp pairing, 8 Pitfalls (WhatsApp deployment without reading the chapter, first-draft family-group delivery, missing test users, running OAuth on the VPS, missing per-calendar remind flag, scope upgrades without re-auth, Workflowy routing check silent failure, conservative schedule-change keyword filter). WhatsApp side is explicitly marked optional with a path to deploy without it. Voice grips clean after one `\bwe\b` fix (quoted example message reworded). |
| 07-4 | — | net-new | 07-4 | `07-4-sergeant-murphy.md` | **done** | Phase 7d | Drafted from `research/agent-history-meetings-coach.md` — the 5x resend incident is the narrative spine, promoted to a dedicated section that covers the silent prior (2026-04-08, five duplicate debriefs on the same meeting, discovered only during post-mortem), the loud incident (2026-04-14, five debriefs across 2.5 hours in the Telegram channel), the root cause (cron prompt treated `cache/pending-debrief-*.json` as a delivery queue + grepped `commitments/active.md` for dedup, but commitments only land after `/confirm`, so unconfirmed meetings re-sent every 30 minutes), the fix (commit `74c726c` — delivery gated only on current run's `transcript-scan.py` output, `processed-transcripts.json` as the sole dedup source, `test_post_meeting_scan_idempotency.py` as regression guard), and the design rule codified in `feedback_cron_delivery_source.md` that now applies to every staging-and-confirm flow in the fleet. Structure: TL;DR, Meet the agent (four mission items), Why you'd want / skip, What makes this hard (four items — post-meeting idempotency, MCP integration, pre-meeting brief determinism, routing boundary), The 5x resend incident (silent prior / loud incident / root cause / fix / design rule), The MCP transcription integration (auth pattern, manual re-auth on 401, 90-minute alert rate-limit from commit `e599fe7`, `get_multiple_documents` gotcha from commit `bb33091`), Routing boundary with Mistress Mouse (cross-ref to Ch 07-3), Current state (5 host crons + 10 I/O scripts + workspace layout), Deployment walkthrough with Google Cloud + transcription vendor + Workflowy pre-steps, 8 Pitfalls (cache-as-queue, append-without-grep, brief learning to compose, OAuth on the VPS, 401-as-retry, confirm double-write, Workflowy session expiration, skipping coaching dry-run). Vendor name genericized to "transcription provider" + `transcript-*` filenames per voice rule (no Krisp brand mentions). Counterparty names from the incidents genericized to "the operator" + "a mid-morning meeting." Voice grips clean on first pass after the Krisp sweep. |
| 07-5 | — | net-new | 07-5 | `07-5-huckle-cat.md` | **done** | Phase 7d | Drafted from `research/agent-history-connector.md` — the brain-first architecture is the narrative spine. Huckle Cat inverts the usual agent shape: instead of wrapping a single external API, he is built around the shared brain itself, mining seven disparate data sources (Gmail, GCal, Google Contacts, transcription provider MCP, WhatsApp, Google Messages, Workflowy) to produce a relationship intelligence layer. Structure: TL;DR, Meet the agent (three jobs — relationship memory, morning nudge, notes triage), Why you'd want / skip, What makes this hard (three items — brain-first deployment, seven-source data-quality fanout, stale-dates failure mode), The mining pipeline (three phases — mine, aggregate+LLM-enrich, review+finalize — with detailed per-miner table and the "why local, not VPS" subsection), The Google Messages + DevTools story (the canonical "no API? hand-rolled JS snippet from a persistent-profile browser" pattern, with gmessages-auth.py + gmessages-mine.py + graceful selector fallback), The stale-dates bug and the daily-refresh fix (narrative section covering the two-day-after-deploy incident where frozen `last_interaction` timestamps caused the morning nudge to flag contacts the operator had just texted, the commit `fa3f785` fix, the 6-attendee cap on transcript refresh to prevent all-hands meetings from silently clearing the overdue list, and the cron-ordering slack between daily-refresh and morning-nudge), Current state (4 host crons + workspace layout + brain state), Deployment walkthrough with step 0 = mining pipeline run locally with human review pass, 8 Pitfalls. Transcription vendor name genericized throughout. LLM cost estimates given as escaped backslash-dollar values to satisfy voice grip and keep the numbers concrete. Voice grips clean on first pass. |
| 07-6 | — | net-new | 07-6 | `07-6-hilda-hippo.md` | **done** | Phase 7d | Drafted from `research/agent-history-shopping.md` — the four-act Costco saga narrated from the Timeline: Act I (cold-start collapse, persistent daemon pivot, April 5-9), Act II (B2C public-client discovery, April 12), Act III (2010 setCookie bug, April 12), Act IV (stabilization, April 12-15). Amazon side covered as the less-dramatic counterpart (curl_cffi + amazonorders + Camoufox+auto-MFA). Pre-step deploy coverage: residential proxy requirement, TOTP secret split between .env and secrets.env, system deps install. Six Pitfall callouts cover the most-likely first-deploy traps. "What Hilda cannot do yet" section is honest about pagination limits, Amazon new-item search not implemented, and credential-reauth fallback fragility. Calibration chapter for the remaining five per-agent chapters — voice is now pinned. |
| 07-7 | — | net-new | 07-7 | `07-7-auth-architectures.md` | **done** | Phase 7d | Drafted as the cross-cutting reference chapter after Ch 07-1 through 07-6 had landed. Structure: TL;DR, The three cross-cutting idioms (local-then-SCP, cache/ gitignored, no raw API keys in cron-invoked scripts), The six auth shapes with canonical references back to the per-agent chapters: Shape 1 (OAuth 2.0 long-lived refresh token — Google Cluster), Shape 2 (OAuth 2.1 rotation-prone refresh token — transcription provider), Shape 3 (bearer token in `.env` — Telegram, Workflowy, residential-proxy creds), Shape 4 (Playwright persistent profile — LinkedIn), Shape 5 (Camoufox + residential proxy + auto-MFA — Costco + Amazon), Shape 6 (QR-code pairing — WhatsApp Baileys + Google Messages). Per-agent reference table crosses all 7 agent chapters against the 6 shapes so readers can see which shapes each agent uses at a glance. 8 Pitfalls (browser auth on the VPS, committing tokens, raw API keys for LLM calls, missing OAuth test-users registration, treating Shape 2 401 as retryable, asking operator to manually re-auth Shape 5, sharing Shape 6 session across agents, forgetting SCP is the sync path not git pull). Companion Pitfall #6 reinforces the Hilda Hippo auto-MFA rule. Companion Pitfall #7 reinforces the Mistress Mouse one-account-per-Shape-6-agent rule. Voice grips clean after one `\bwe\b` fix (a quoted vendor-side phrase reworded to avoid the grip). |
| 07-8 | — | net-new | 07-8 | `07-8-the-inbox.md` | **done** | Phase 7d | Net-new chapter documenting the conversational inbox architecture (Phases A-D). Covers the five cross-cutting components (inbox daemon, dispatcher, tool-use loop, conversation window, pending-actions store), the per-agent tools.py pattern (read tools / producer tools / confirm executors), the inline-button UX (single action, batch, engagement callbacks), the deployment story (systemd user unit + kill-switch + install script), 9 locked-in architectural decisions, and 8 pitfalls. Per-agent tool inventory table crosses all six agents. |
| 08 | — | net-new | 08 | `08-security-and-hardening.md` | **done** | Phase 7d | Drafted as the security chapter for the Clawford-native stack. Structure: TL;DR, The threat model (five named threats in priority order — agent drift, accidental output, credential leak via git, trust erosion, active attack last), Defense layer 1 (OS-level immutability via chattr +i on SOUL/IDENTITY/TOOLS/AGENTS with the soft-constraint-trap discussion), Defense layer 2 (the script contract — exit 0 always, one JSON line on stdout, no shell — with the deterministic envelope around LLM calls), Defense layer 3 (the deploy.py safeguards table with all 9 active safeguards + the two retired tombstones 8 and 11), Credential storage (brief, delegates to Ch 07-7), The liberation rationale (why the gateway-era allowlist was doing double duty as both a security and behavioral boundary and why the split is clearer post-liberation), What is NOT defended (6 honest gaps — no inter-agent sandboxing, no rate-limiting, no Prometheus/Grafana, no secret rotation automation, no pip supply-chain defense, no protection against local-laptop compromise), 6 Pitfalls (relying on soft constraints, breaking script contract "just once," adding safeguards without understanding existing ones, assuming exec-approvals allowlist still exists, manual chattr -i without the restore, treating fleet-health output as informational). Voice grips clean on first pass. |
| 09 | — | net-new | 09 | `09-scripts-and-configs.md` | **done** | Phase 7d | Categorical + alphabetical reference chapter drafted against the live `agents/shared/` and `ops/scripts/` inventory. Organized by what-it-does rather than where-it-lives: Category 1 shared library (Tier 1 clean APIs + Tier 2 Playwright + Tier 3 Camoufox + Ops modules), Category 2 deploy-and-install tooling (install-host-cron, install-host-deps, install-host-system-deps, sync-brain-to-vps, set-bot-commands, set-bot-descriptions), Category 3 ops helpers and host-cron wrappers (fleet-health, morning-fleet-deliver, news-digest-morning-edition, fix-it-cron-self-check, costco-token-refresh, script-contract, plus the non-cron fleet-health.py + probe-agent.py + crlf-scan.py + their test files), Category 4 per-agent script conventions (required files table + script-naming-convention table covering `-auth.py` / `-fetch.py` / `-mine.py` / `-check.py` / `-alert.py` / `-host.sh` / `heartbeat.py`), Category 5 config file schemas (manifest.json full example, .env common-keys table, identity file conventions). Alphabetical index covers all 34 files for "I know the name but not the category" lookups. Every table row cross-refs back to the chapter that explains the deeper context. Voice grips clean on first pass. |
| 10 | — | *retired* | — | — | **retired** | Phase 7d | Chapter retired in Phase 7d. Post-liberation there is no Clawford CLI — the operator drives the fleet via `python3 agents/shared/deploy.py`, `python3 ops/scripts/fleet-health.py`, `install-host-cron.sh`, `crontab`, `ssh`, `scp`, and `codex infer`. All of those are already documented in Ch 04 (VPS setup), Ch 07 (deploy path), and Ch 09 (scripts and configs reference). A standalone CLI reference would just duplicate content from those three chapters. Skipping Ch 10 entirely; Ch 11 glossary is the next and last net-new chapter. |
| 11 | — | net-new | 10 | `10-glossary.md` | **done** | Phase 7d | Renumbered from Ch 11 → Ch 10 after Ch 10 (CLI reference) was retired in Phase 7d. Alphabetical A-Z glossary of Clawford-specific terms (~60 entries covering Agent, Baileys, Brain, Busytown, Cache-is-not-a-queue rule, Camoufox, chattr +i, Clawford, codex infer, Commitment, Compose/deliver split, Contract, contract_wrap.py, CRONS.md, deploy.py, Drift detection, .env, Fleet, Fleet delivery path, Fleet health, Heartbeat, HEARTBEAT.md, Host cron, IDENTITY.md, install-host-cron.sh, Liberation, Local-then-SCP, Manifest, MCP, Mining pipeline, Morning brief, OpenClaw, Operator, People file, Persistent profile, PKCE, Post-liberation, Pre-liberation, Residential proxy, Review-before-finalize, Safeguard, Script contract, Shared brain, Shared library, Smoke test, SOUL.md, Soft constraint, Sticky port, T-10 slack, TOTP, Tier 1/2/3, TOOLS.md, Workflowy routing rule, Workspace). Plus two dedicated tail sections: **Named incidents** (five load-bearing war stories referenced by name across multiple chapters — 5x resend, Costco saga, probation episode, smart-reply chip, stale-dates bug) and **Retired terms** (eight retired terms flagged as historical so readers of old commit messages, older documentation, or frozen guide-v2 content can find meanings without assuming they refer to live infrastructure — exec-approvals allowlist, OpenClaw, openclaw.json, oc/oci CLI, 600-second LLM cron timeout, ~/.openclaw/, ~/openclaw/, Safeguards 8 and 11). Voice grips clean on first pass. |

## Notes

- **v2 stays frozen.** After Phase 7, a prominent frozen-notice at the top of `guide-v2/index.md` points readers to v3 as the live guide. v2 files themselves are not deleted — they are the historical record.
- **Rewrites cannibalize.** "Rewrite" means the v2 version isn't being ported wholesale, but its concrete facts, file paths, and scar-tissue incidents are fair game for the v3 version. The structural shape changes; the underlying knowledge survives.
- **Cross-references.** During migration, v3 chapters may need to link to v2 chapters that haven't been migrated yet, and vice versa. Prefer linking within v3 where possible; fall back to v2 only for chapters that are explicitly "to migrate later."
- **Update this table's Status column as each chapter completes.** The table is the canonical tracking doc for the migration.
